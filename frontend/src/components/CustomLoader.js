import React from 'react';
import '../assets/css/CustomLoader.css'; // Importing loader CSS

const CustomLoader = () => {
  return (
    <div className="loader-container">
      <div className="custom-loader"></div>
    </div>
  );
};

export default CustomLoader;
